#include "miniwin.h"
using namespace miniwin;

int main(){
	vredimensiona(1000,1000);
    rectangulo_lleno(0,0,,);//rectangulo
    system("pause");
    borra();
    circulo_lleno(50,50,);//circulo
    system("pause");
    borra();
    linea(0,0,,);
    linea(0,,,);//triangulo
    system("pause");
    borra();
    refresca();
	return 0;
}
